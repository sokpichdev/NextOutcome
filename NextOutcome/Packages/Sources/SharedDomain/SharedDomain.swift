// SharedDomain module — Phase 0 placeholder
//
/// This file has no code of its own — it exists so the `SharedDomain` Swift Package
/// target has at least one source file (Swift Package Manager requires every target
/// to contain at least one file to build). The real content of this module lives in
/// `LoadState.swift` and `Page.swift`, which define types shared across every
/// feature package (Markets, Orderbook, Portfolio, Trading, LiveStats) so they don't
/// each reinvent loading-state or pagination types.
