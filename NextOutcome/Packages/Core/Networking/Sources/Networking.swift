// Networking module — Phase 0 placeholder
//
/// This file has no code of its own — it exists so the `Networking` Swift Package
/// target has at least one source file. The real content of this module is spread
/// across the other files in this directory: `APIClient` (making requests),
/// `Endpoint`/`HTTPMethod` (describing requests), `APIError` (error handling),
/// `RetryPolicy` (backoff behavior), `NetworkLogger` (debug logging), and the
/// `Decoding/` subfolder (JSON parsing helpers for Polymarket's API quirks).
